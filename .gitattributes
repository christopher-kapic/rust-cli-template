# Normalize line endings so checkouts are consistent across OSes.
* text=auto eol=lf

# Shell and PowerShell installers must keep their native endings.
*.sh   text eol=lf
*.ps1  text eol=crlf

# Lockfile: treat as generated in diffs/reviews (still committed & tracked).
Cargo.lock linguist-generated=true
